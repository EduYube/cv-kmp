build:
  maxIssues: 0

config:
  validation: true

style:
  MagicNumber:
    active: false

complexity:
  LongMethod:
    active: true
    threshold: 60

naming:
  FunctionNaming:
    active: true
    functionPattern: '([a-z][a-zA-Z0-9]*|[A-Z][a-zA-Z0-9]*)'
